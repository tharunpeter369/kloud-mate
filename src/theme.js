import { createTheme } from "@mui/material/styles";
// import { green, purple } from "@mui/material/colors";

export const theme = createTheme({
  palette: {
    primary: {
      // main: "#2C9ADF",
      main: "#ffffff",
    },
    secondary: {
      // main: "rgb(255, 149, 31)",
      main: "#ffffff",
    },
    third: {
      main: "rgb(206, 146, 216)",
    },
  },
});
